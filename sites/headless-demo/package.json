{
	"private": true,
	"name": "headless-demo",
	"version": "0.0.1",
	"type": "module",
	"description": "Reference example: chiqui used headlessly (content engine only, own layout, own adapter) inside an otherwise-unrelated dynamic app — see AGENTS.md/README's \"Headless usage\" section.",
	"scripts": {
		"dev": "vite dev",
		"build": "vite build",
		"check": "svelte-kit sync && svelte-check --tsconfig ./tsconfig.json",
		"prepare": "svelte-kit sync || echo ''"
	},
	"dependencies": {
		"@mrmx/chiqui": "workspace:*"
	},
	"devDependencies": {
		"@sveltejs/adapter-auto": "^6.1.1",
		"@sveltejs/kit": "^2.47.0",
		"@sveltejs/vite-plugin-svelte": "^6.2.1",
		"svelte": "^5.40.1",
		"svelte-check": "^4.3.3",
		"typescript": "^5.9.3",
		"vite": "^7.1.10"
	}
}
